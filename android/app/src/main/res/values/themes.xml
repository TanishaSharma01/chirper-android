<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (Light Mode) -->
    <style name="Base.Theme.Hackathon" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/accentGreen</item>
        <item name="colorPrimaryVariant">@color/accentGreenDark</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/accentOrange</item>
        <item name="colorSecondaryVariant">@color/accentOrangeDark</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Background colors -->
        <item name="android:colorBackground">@color/background</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/primaryText</item>
        <item name="colorOnBackground">@color/primaryText</item>

        <!-- Status bar (light mode) -->
        <item name="android:statusBarColor">@color/accentGreenDark</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.Hackathon" parent="Base.Theme.Hackathon" />
</resources>
